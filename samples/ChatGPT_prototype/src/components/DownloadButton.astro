---
interface Props { href?: string; label?: string; size?: string }
const { href, label = 'Download companion', size } = Astro.props;
---
{href ? <a class="button" href={href} download>{label}{size && <span class="button-size">({size})</span>}</a> : <span class="button disabled">Companion coming soon</span>}
